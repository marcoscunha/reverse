#include <libelf.h>
#include <libdwarf.h>
#include <dwarf.h>


/**
* @brief 
*
* @param elf
*
* @return 
*/
/*dwarf_t * dwarf_init(Elf *elf)
{
    dwarf_t *res = malloc(sizeof(dwarf_t));
    res->dwarf     = NULL;

    Dwarf_Error err;
    int ret;
*/
    /* Dwarf init */
/*    ret = dwarf_elf_init(elf, DW_DLC_READ,
                         dwarf_on_error, res,
                         &(res->dbg), &err);

    if(ret == DW_DLV_ERROR) {
        ERROR("Dwarf2 initialisation error\n");
        exit(EXIT_FAILURE);
    }

    printf("Dwarf Init done.\n");

    return res;
   
}*/


